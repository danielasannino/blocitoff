<%= form_for item do |f| %>
    <%= f.text_field :name, class: 'form-control', placeholder: "Enter new to-do list item" %>
    <div class="form-group", style="padding-top:10px;">
        <%= f.submit "Save", class: 'btn btn-success' %>
    </div>
<% end %>
